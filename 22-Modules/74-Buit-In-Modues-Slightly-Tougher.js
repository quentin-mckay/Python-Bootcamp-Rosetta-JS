/* 
there's no equivalent of keyword.iskeyword in javascript
so two potential solutions would be 
1. storing all possible keywords in an array
2. using eval("let ${possible_keyword} = 0") and test for an error (very hacky)
*/

