instructors = []

# add multiple items
instructors.extend(["Colt", "Blue", "Lisa"])

# remove last
instructors.pop()

# remove first
instructors.pop(0)

# insert at beginning
instructors.insert(0, 'Done')

print(instructors)